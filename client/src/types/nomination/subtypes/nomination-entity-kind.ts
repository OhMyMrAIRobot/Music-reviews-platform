/**
 * Discriminator for the kind of entity that can be nominated.
 *
 * - `'author'` — nomination targets an author
 * - `'release'` — nomination targets a release
 */
export type NominationEntityKind = 'author' | 'release'
