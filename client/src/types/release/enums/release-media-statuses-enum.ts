/**
 * ReleaseMediaTypesEnum
 *
 * Human-readable labels for different media items attached to a
 * release, used in API responses and UI.
 */
export enum ReleaseMediaStatusesEnum {
  PENDING = 'Ожидание',
  APPROVED = 'Принято',
  REJECTED = 'Отклонено',
}
