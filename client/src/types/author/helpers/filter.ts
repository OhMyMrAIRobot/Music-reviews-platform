/** Enum of filter options for author types used in UI controls.
 *
 * Use these values when rendering filter labels or comparing selected filter values.
 */
export enum AuthorTypesFilterOptions {
	ALL = 'Все',
	ARTIST = 'Артист',
	PRODUCER = 'Продюссер',
	DESIGNER = 'Дизайнер',
}
