/**
 * UpdateAuthorConfirmationData — payload for updating an author confirmation.
 */
export type UpdateAuthorConfirmationData = {
  /** Target status id to set on the confirmation. */
  statusId: string;
};
