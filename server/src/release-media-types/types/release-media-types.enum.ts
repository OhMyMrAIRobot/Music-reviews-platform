/**
 * ReleaseMediaTypesEnum
 *
 * Human-readable labels for different media items attached to a
 * release, used in API responses and UI. Use these enum members when
 * working with media-type values on the server.
 */
export enum ReleaseMediaTypesEnum {
  MUSIC_VIDEO = 'Клип',
  MEDIA_REVIEW = 'Медиарецензия',
  MEDIA_MATERIAL = 'Медиаматериал',
  SNIPPET = 'Сниппет',
}
